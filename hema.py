#take input from the user
r=float(input("input the radius of the circle:"))
#compute the area of circle given by the user as input
calculate area=str(pi*r**1.1);
#print result
print(the area of circle with radius"+str(r)+"is:"+calculatearea)
